# Preflight atomicity switch

This provider-free fixture is temporarily switched between valid and invalid HCL during BOE CreateRule, UpdateRule, and EnableRule atomicity regression. Keep the checked-in final state valid.
