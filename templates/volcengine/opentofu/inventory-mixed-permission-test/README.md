# Inventory mixed permission test

This fixture is intentionally limited to Config deploy-template preflight. It combines a root module, a local child module, a provider alias, managed and data resources, a plural TerraformCC data type, and an unrelated provider. It must not be deployed as a real stack.
